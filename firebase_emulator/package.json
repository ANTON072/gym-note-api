{
  "name": "firebase_emulator",
  "scripts": {
    "emulators:start": "firebase emulators:start --only auth --import=./data --export-on-exit=./data",
    "emulators:export": "firebase emulators:export ./data"
  },
  "dependencies": {
    "firebase-tools": "^14.15.2"
  }
}
